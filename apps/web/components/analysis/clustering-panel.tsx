"use client";

export function ClusteringPanel() {
  return (
    <section className="space-y-4 p-8">
      <h2 className="text-2xl font-semibold">Semantic Keyword Clustering</h2>
      <p>Upload CSV, paste keywords, or import Search Console data to create SEO content silos.</p>
      <div className="rounded-xl border p-4 text-sm">
        Cluster cards, topic hierarchy tree, and semantic graph visualizations are rendered here.
      </div>
    </section>
  );
}
